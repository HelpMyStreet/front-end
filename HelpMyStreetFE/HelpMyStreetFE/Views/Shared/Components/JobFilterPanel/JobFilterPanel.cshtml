@model HelpMyStreetFE.Models.Account.Jobs.JobFilterViewModel

<form action="/api/requesthelp/get-filtered-jobs" method="post">
    <h6>Filter jobs here</h6>

    @if (Model.JobStatuses != null)
    {
        <fieldset>
            <p>Status:</p>
            @foreach (var status in Model.JobStatuses)
            {
                <label for="filter-@status.Value">@status.Value (@status.MatchingItems)</label>
                <input name="JobStatuses[]" type="checkbox" id="filter-@status.Value" value="@((int)status.Value)" checked="@status.IsSelected" />
            }
            <label for="filter-status-select-all">Select all</label>
            <input id="filter-status-select-all" type="checkbox" class="filter-select-all" />
        </fieldset>
    }

    @if (Model.SupportActivities != null)
    {
        <fieldset>
            <p>Activity:</p>
            @foreach (var supportActivity in Model.SupportActivities)
            {
                <label for="filter-@supportActivity.Value">@supportActivity.Value.FriendlyName() (@supportActivity.MatchingItems)</label>
                <input name="SupportActivities[]" type="checkbox" id="filter-@supportActivity.Value" value="@((int)supportActivity.Value)" checked="@supportActivity.IsSelected" />
            }
            <label for="filter-supportActivity-select-all">Select all</label>
            <input id="filter-supportActivity-select-all" type="checkbox" class="filter-select-all" />
        </fieldset>
    }

    @if (Model.MaxDistanceInMiles != null)
    {
        <fieldset>
            <p>Distance:</p>
            @foreach (var distance in Model.MaxDistanceInMiles)
            {
                <label for="filter-@distance.Value">@distance.Label (@distance.MatchingItems)</label>
                <input type="radio" name="MaxDistanceInMiles" id="filter-@distance.Value" value="@distance.Value" checked="@distance.IsSelected" />
            }
        </fieldset>
    }

    @if (Model.DueInNextXDays != null)
    {
        <fieldset>
            <p>Due:</p>
            @foreach (var dueInNextXDays in Model.DueInNextXDays)
            {
                <label for="filter-@dueInNextXDays.Value">@dueInNextXDays.Label (@dueInNextXDays.MatchingItems)</label>
                <input type="radio" name="DueInNextXDays" id="filter-@dueInNextXDays.Value" value="@dueInNextXDays.Value" checked="@dueInNextXDays.IsSelected" />
            }
        </fieldset>
    }

    <button type="submit" class="update">Update</button>
</form>