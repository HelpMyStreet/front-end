@model CountNavViewModel
@using HelpMyStreetFE.Enums.Account;

    <a href="/account/accepted-requests" class="@(Model.CurrentPage == MenuPage.AcceptedRequests ? "selected" : string.Empty)">My Accepted Requests @if(Model.Count != 0) {<span class="count">@Model.Count</span>}</a>